spring:
  config:
    import: configserver:http://localhost:8888 # This file is the entrypoint for spring to know where to look for the configuration file and hold the initial configuration
  application:
    name: eureka-service # name of the applications
  cloud:
    config:
      fail-fast: true # the s